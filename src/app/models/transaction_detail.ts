export interface TransactionDetail
{
    amount: string
    attachments: string
    blob_size: string
    block_hash: string
    block_timestamp: string
    extra: string
    fee: string
    id: string
    ins: string
    keeper_block: string
    outs: string
    pub_key: string
    timestamp: string
}